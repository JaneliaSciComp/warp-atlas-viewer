node_modules
dist
.DS_Store
*.log
playwright-report/
test-results/

.claude
# Git worktrees checked out inside the repo. Untracked, they get staged as
# embedded gitlinks by `git add -A`; ignored, `vitest` still collects each
# copy's test files, so scope runs with `--dir src`.
/.worktrees/
/data/
/preprocessed/
scripts/.env.download

# Per-developer local env (e.g. dev-server allowedHosts).
# See .env.local.example for the format.
/.env.local
/.env*.local
CodeReview*.md

# VitePress build output and cache
docs/.vitepress/dist
docs/.vitepress/cache
