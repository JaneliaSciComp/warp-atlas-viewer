## Git Workflow
- Automatically create a git commit after completing each logical unit of work.
- Use conventional commit format (e.g., "feat:", "fix:").
- Do not wait for user permission to commit.

